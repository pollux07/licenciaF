{% extends '::base.html.twig' %}

{% block body -%}
    <h1>avisos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Aviso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('avisos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.aviso }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('avisos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
